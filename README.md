# Squire

Public promotional site and release downloads for Squire.

The game packages include the executable, artwork, audio, and runtime files. The optional local dialogue model is distributed separately because it is too large for the site package; see the model note on the download page and in each package's `refs/models/README.txt`.
